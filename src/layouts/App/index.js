import React, { Component } from 'react'
import { Header } from '../Header'
import { Tab } from '../Tab'

export const App = (props) => {
  return (
    <div>
    	<Header />
			<Tab />
  	</div>
  )
}
